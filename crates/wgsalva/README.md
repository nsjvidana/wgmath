# wgsalva: cross-platform GPU fluid simulation

**/!\ This library is still under heavy development and is still missing many features.**

The goal of **wgsalva** is to especially be "**salva** on the gpu". It aims (but it isn’t there yet) to be a
GPU particle-based fluid physics engine.
